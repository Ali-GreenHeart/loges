import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#FFBE34'
        },
        secondary: {
            main: '#091242'
        }
    }
})

export default theme;
